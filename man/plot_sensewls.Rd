% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sensewls.R
\name{plot_sensewls}
\alias{plot_sensewls}
\title{Contour plot method for sensewls objects}
\usage{
plot_sensewls(
  sensewls_obj,
  xlim = c(0, 1),
  ylim = c(0, 1),
  sig_contour = FALSE,
  sig_alpha = NULL,
  palette = 7
)
}
\arguments{
\item{sensewls_obj}{An object of class \code{sensewls}, constructed from the \code{sensewls} function}

\item{xlim}{A vector providing the lower and upper bounds of the x-axis.
Must be a subinterval of the unit interval}

\item{ylim}{A vector providing the lower and upper bounds of the y-axis.
Must be a subinterval of the unit interval.}

\item{sig_contour}{A single logical, indicating whether or not to plot the contour line for the \emph{first}
adjusted estimate that is statistically insignificant (at the \code{sig_alpha} level). Only allowed if bootstrap inference was used when creating \code{sensewls_obj}. Default value is \code{FALSE}.}

\item{sig_alpha}{Number between 0 and 1. Updated level of significance for the \code{sig_contour} line. If \code{NULL}, then assumes the significance level from provided \code{sensewls_obj}. Default value is \code{NULL}.}

\item{palette}{An argument which is supplied to \code{scale_color_brewer} to
color the scatterplot with a seqential palette.}
}
\value{
Contour plot
}
\description{
Forms a contour plot of the adjusted estimates for values of the unknown partial
correlations between a confounder and treatment and the confounder and outcome.
}
