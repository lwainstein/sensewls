% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensewls.R
\name{sensewls}
\alias{sensewls}
\title{Sensitivity analysis to omitted confounders for weighted least squares estimators of treatment effects}
\usage{
sensewls(
  df,
  treatment,
  outcome,
  covars,
  bounding_covars,
  kd = 1,
  ky = kd,
  w,
  semiweights = NULL,
  normalize = FALSE,
  estimand = NULL,
  inference = TRUE,
  alpha = 0.05,
  cluster = NULL,
  B = 500,
  par = F,
  ncpus = NULL,
  ...
)
}
\arguments{
\item{df}{A dataframe containing \code{treatment}, \code{outcome}, and \code{covars}.}

\item{treatment}{A string indicating the column name of the treatment variable.}

\item{outcome}{A string indicating the column name of the outcome variable.}

\item{covars}{A character vector of column names of the relevant covariates in the dataframe.}

\item{bounding_covars}{A list of character vectors containing covariates to benchmark by. May be a character vector if only one set of bounding covariates is desired -- note in this case that \code{sensewls()} will benchmark on all of the covariates in the provided vector \emph{combined}, and \emph{not} separately.}

\item{kd}{A numeric vector of considered covariate bound strengths for association with the treatment variable. Default value is \code{1}.}

\item{ky}{A numeric vector of considered covariate bound strengths for association with the outcome variable. Default value is \code{kd}.}

\item{w}{A weights object, containing either
(1) a numeric vector of weights
(2) an appropriate weighting function, or
(3) a string for one of the preset weighting functions.
Three presets are currently available, \code{prop_score} (inverse propensity score weights, with a logistic regression for the treatment variable), \code{matchit} (through the \code{MatchIt} package: https://cran.r-project.org/web/packages/MatchIt/index.html)
and \code{ebal} (through the \code{ebal} package: https://cran.r-project.org/web/packages/ebal/index.html).}

\item{semiweights}{List of semiweights, with the ith entry corresponding
to the semiweights formed when constructing weights while omitting the
ith set of covariates in the \code{bounding_covars} list. May be one numeric vector of semiweights if \code{bounding_covars} is only one character vector of bounding covariates.
Defaults to \code{NULL}. \code{semiweights} should be \code{NULL} unless \code{w} is a numeric vector of specified weights.}

\item{normalize}{A single logical, indicating whether or not to normalize the weights (and \code{semiweights}) when either (1) a numeric vector of weights, or (2) a custom weight function is provided for \code{w}.
Default is \code{FALSE}. But weights are normalized for all the preset weighting functions.}

\item{estimand}{A string indicating the desired estimand. Only required if using preset weights. Can be \code{"ATT"}, \code{"ATC"}, or \code{"ATE"}. Default is \code{NULL}.}

\item{inference}{Whether to obtain uncertainty estimates. If set to the logical
\verb{"TRUE}, then a non-parametric bootstrap is performed that fixes the weights, and samples them
along with the data in each bootstrap sample. If set to the character \code{"reestimate"},
a non-parametric bootstrap is performed that re-estimates the weights in each bootstrap sample. This is only allowed if
a preset weighting function or an appropriate, user-supplied weighting function is chosen for \code{w}.
The percentile method is used for all bootstrap confidence intervals.
If set to the logical \code{FALSE}, then no uncertainty estimates are provided. Default is \code{TRUE}.}

\item{alpha}{Number between 0 and 1. Level of significance used for RV_alpha, and (1 - alpha)*100\% confidence intervals.
Defaulted to alpha = 0.05 (and 95\% confidence intervals).}

\item{cluster}{An optional string indicating a column from the data frame \code{df} for which to perform a cluster bootstrap if desired.
\code{inference} must be set to \code{TRUE} or \code{"reestimate"}. Defaulted to \code{NULL}.}

\item{B}{Number of bootstrap samples if used. Defaulted to 500.}

\item{par}{Boolean for whether bootstrapping should be parallelized through
the \code{parallel} library. Defaulted to \code{FALSE}.}

\item{ncpus}{Number of cpus used if \code{par} is set to \code{TRUE}. Defaulted to \code{NULL} and
must be supplied if \code{par} is set to true.}

\item{...}{Additional arguments passed to preset weighting functions.
Currently supports providing \code{caliper} and \code{ratio} to the \code{\link[MatchIt:matchit]{MatchIt::matchit()}} weighting function
used by setting \code{w = 'matchit'}.}
}
\value{
A sensewls object containing a variety of sensitivity statistics. The output object can be plotted using the
\code{plot_sensewls()} function. Significance levels and \code{q} threshold for robustness values and confidnece intervals
can be changed by applying the \code{alter_sensewls()} function.
}
\description{
This function performs the weighted sensitivity analysis for a weighted least squares
estimator as described in \href{https://www.arxiv.org/abs/2508.02954}{Wainstein and Hazlett (2025)}. It returns an object of class \code{sensewls}
containing a variety of sensitivity statistics. An output object can use the \code{plot_sensewls()}
function for visualization.
}
